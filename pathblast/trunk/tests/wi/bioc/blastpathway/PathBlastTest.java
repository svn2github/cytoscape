

package wi.bioc.blastpathway; 

import junit.framework.*;

public class PathBlastTest extends TestCase {

    protected void setUp() {
	}

	public void testExample1() {
/*
        Protein[] proteins = new Protein[3];
        proteins[0] = Protein.createProtein("MYS1_YEAST"); // Myo1
        proteins[1] = Protein.createProtein("ACT_YEAST"); // Act1
        proteins[2] = Protein.createProtein("ST20_YEAST"); // Ste20
        String t_org = Config.T_ORG_VALUES[0];
        Evalue nEvalue = new EValue(EValue.DEFAULT_EVALUE);

		session.setAttribute(Config.PROTEINS_SESSION_KEY, proteins);
		session.setAttribute(Config.EVALUE_SESSION_KEY, nEvalue);
		session.setAttribute(Config.USE_ZERO_SESSION_KEY, useZero);
		session.setAttribute(Config.BLAST_ALL_DIP_SESSION_KEY, blastAllDip);
		session.setAttribute(Config.TORG_SESSION_KEY, t_org);
*/
		assertTrue(true);
		
	}
}
