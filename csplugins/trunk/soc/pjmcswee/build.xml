


<project name="RandomNetworkPlugin" default="dist" basedir=".">
    <description>
        Creates our Random Network Plugin
    </description>

  <!-- set global properties for this build -->
  <property name="src" location="src"/>
  <property name="build" location="build"/>
  <property name="dist"  location="dist"/>

  <target name="init">
    <!-- Create the time stamp -->
    <tstamp/>
    <!-- Create the build directory structure used by compile -->
    <mkdir dir="${build}"/>
  </target>

  <target name="compile" depends="init"
        description="compile the source " >
    <!-- Compile the java code from ${src} into ${build} -->
    <javac srcdir="${src}" destdir="${build}" classpath="../../cytoscape/cytoscape.jar"/>
  </target>

  <target name="dist" depends="compile"
        description="generate the distribution" >
    <!-- Create the distribution directory -->
    <mkdir dir="${dist}/lib"/>

    <!-- Put everything in ${build} into the MyProject-${DSTAMP}.jar file -->
    <jar jarfile="${dist}/lib/MyProject-${DSTAMP}.jar" basedir="${build}"/>
  </target>

  <target name="clean"
        description="clean up" >
    <!-- Delete the ${build} and ${dist} directory trees -->
    <delete dir="${build}"/>
    <delete dir="${dist}"/>
  </target>
  
  <target name="jar" description="Create RandomNetworkPlugin jar file" depends="compile">
    <copy file="${basedir}/plugin.props" todir="build/cytoscape/randomnetwork" />
    <jar destfile="RandomNetworkPlugin.jar">
         <manifest>
         <attribute name="Cytoscape-Plugin"
                    value="cytoscape.randomnetwork.RandomNetworkPlugin" />
         </manifest>

         <fileset dir= "${build}" />
    </jar>
</target>
  
</project>



