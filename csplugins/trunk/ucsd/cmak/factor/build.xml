<project name="mdi" default="compile" basedir=".">
    <description>
        simple example build file
    </description>
  <!-- set global properties for this build -->
  <property name="src" location="src"/>
  <property name="build" location="build"/>
  <property name="dist"  location="dist"/>
  <property name="lib"  location="lib"/>
  <property name="doc"  location="doc"/>


   <path id="project.class.path">
       <fileset dir="../jars">
	   <include name="*.jar"/>
       </fileset>
       <pathelement location="./${build}"/>
    </path>



  <target name="init">
    <!-- Create the time stamp -->
    <tstamp/>
    <!-- Create the build directory structure used by compile -->
    <mkdir dir="${build}"/>

  </target>

  <target name="doc">

    <!-- Create the build directory structure used by compile -->
    <delete dir="${doc}"/>
    <mkdir dir="${doc}"/>
    <javadoc sourcepath="${src}" destdir="${doc}">
       <fileset dir="${src}">
	  <exclude name="**/*Test*"/>
        </fileset>
    </javadoc>
  </target>


  <target name="compile" depends="init"
        description="compile the source " >
    <!-- Compile the java code from ${src} into ${build} -->
    <javac srcdir="${src}" destdir="${build}" deprecation="no">
         <classpath refid="project.class.path"/>
    </javac>

  </target>

   <!-- does not work for some reason -->
  <target name="test">
         <junit printsummary="on" showoutput="false">
           <classpath refid="project.class.path"/>
	   <test name="ExpressionTest"/>
	   <test name="MaxProductTest"/>
	   <test name="DFSPathTest"/>
	   <test name="PathTest"/>
	   <test name="ProbTableTest"/>
	   <test name="VariableNodeTest"/>
	   <test name="FactorNodeTest"/>
	   <test name="FGTest"/>
	   <test name="ChiTest"/>
	 </junit>
  </target>

  <target name="dist" depends="compile"
        description="generate the distribution" >
    <!-- Create the distribution directory -->

    <!-- Put everything in ${build} into the MyProject-${DSTAMP}.jar file -->
    <jar jarfile="${dist}/lib/mdi-${DSTAMP}.jar" basedir="${build}"/>
  </target>

  <target name="clean"
        description="clean up" >
    <!-- Delete the ${build} and ${dist} directory trees -->
    <delete dir="${build}"/>
    <delete dir="${dist}"/>
  </target>
</project>