This directory contains the data that is needed by the MCODE Java Web Start demo.  It is packaged into the data.jar file in the javaws
directory using the command:
jar -cvf data.jar *.gml *.props projectFile *.anno annotList *.onto *.syno

The jar also needs to be signed.