<?xml version = "1.0"?>
<project name = "metaNodeViewer" default = "compile" basedir = ".">
   
   <property name = "src" location = "src"/>
   <property name = "build" location = "build"/>
   <property name = "build.compiler" value = "modern"/>
   <property name = "lib" location = "../../../../cytoscape/lib"/>
   <property name = "jar"  location = "jar"/>
   <property name = "doc" location = "doc"/>

   <property name="cytoscape.plugin.dir" value="../../../../cytoscape/plugins"/>

   <path id = "classpath_compile">
       <fileset dir = "${lib}">
          <include name = "**/*.jar"/>
       </fileset>
   </path>

   <path id = "classpath_run">
       <fileset dir = "${lib}">
          <include name = "**/*.jar"/>
       </fileset>
   </path>

   <target name="prepare" depends="clean">
      <mkdir dir="${build}"/>
   </target>

   <target name="compile" depends = "prepare" description="Compile">
   <!-- Compile the java code from ${src} into ${build} -->
   <javac source = "1.4" debug="true" srcdir = "${src}" destdir="${build}">
      <classpath refid = "classpath_compile"/>
   </javac>
   </target>
   
   <target name="clean" description="clean up" >
      <!-- Delete the ${build} and ${src} directory trees -->
      <delete dir="${build}"/>
      <delete dir="${jar}"/>
   </target>

   <target name="jar" depends="compile" description="makes a jar">
   <mkdir dir = "${jar}"/>
   <jar jarfile="${jar}/metanode.jar">
      <fileset dir="${build}"/>
   </jar>
    <copy todir="${cytoscape.plugin.dir}" file="jar/metanode.jar"/>
   </target>

   <target name = "run" depends="jar" description = "Run">
      <java classname = "cytoscape.CyMain" classpathref = "classpath_run"
          fork = "true" maxmemory = "1g">
      	  <arg line = "--JLD"/>
      	  <arg line = "${cytoscape.plugin.dir}"/>
      	  <arg line = "-vt"/>
      	  <arg line = "5000"/>
      </java>
    </target>

   <!-- Target to Run JavaDoc -->
    <target name="doc" description="Runs JavaDoc.">
        <javadoc packagenames="metaNodeViewer.*" sourcepath="src" source="1.4" destdir="doc">
        <link href = "http://java.sun.com/j2se/1.4.2/docs/api/"/>
        <link href = "http://csbi.sourceforge.net/API/"/>
       </javadoc>
    </target>

</project>
